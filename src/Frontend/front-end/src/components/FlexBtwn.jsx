import { Box } from "@mui/material";
import {styled} from "@mui/system";

// for reusing css styled components

const FlexBtwn = styled(Box)({
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center"
})

export default FlexBtwn;